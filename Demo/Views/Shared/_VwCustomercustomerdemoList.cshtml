@model IEnumerable<BusinessModelLayer.Vwcustomercustomerdemo>

<!-- ============================================================== -->
<!-- data table  -->
<!-- ============================================================== -->
<div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
    <div class="card">
        <div class="card-header">
            <h5 class="mb-0">Print, Excel, CSV, PDF Buttons</h5>
            <p>You can use the buttons below to export the data to external sources or change the visibility of the columns.</p>
        </div>
        <div class="card-body">


    <div class="table-responsive">
        <table id="example" class="table table-striped table-bordered second" style="width:100%">
            <thead>
                <tr>
                    <th>
                        @*@Html.DisplayNameFor(model => model.CustomerID)*@
                        @Html.ActionLink("CustomerID", "List", new { ViewBag.PgeSize, sortOrder = ViewBag.CustomerIDSortParm, currentFilter = ViewBag.CurrentFilter })
                    </th>
                    <th>
                        @*@Html.DisplayNameFor(model => model.CustomerTypeID)*@
                        @Html.ActionLink("CustomerTypeID", "List", new { ViewBag.PgeSize, sortOrder = ViewBag.CustomerTypeIDSortParm, currentFilter = ViewBag.CurrentFilter })
                    </th>
                    <th></th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.CustomerID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CustomerTypeID)
                        </td>

                        <td>
                            @Html.ActionLink("Edit", "Edit", new { CustomerID = item.CustomerID, CustomerTypeID = item.CustomerTypeID })
                        </td>
                        <td>
                            @Html.ActionLink("Details", "Details", new { CustomerID = item.CustomerID, CustomerTypeID = item.CustomerTypeID })
                        </td>
                        <td>
							@Html.ActionLink("Delete", "Delete", new { CustomerID = item.CustomerID, CustomerTypeID = item.CustomerTypeID }, new { @class = "btn btn-danger btn-sm" })
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>



        </div>
    </div>
</div>
<!-- ============================================================== -->
<!-- end data table  -->
<!-- ============================================================== -->



