@model PagedList.IPagedList<BusinessModelLayer.Vwcustomercustomerdemo>
@using PagedList.Mvc;
<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />

<tbody>
	@foreach (var item in Model)
	{
		<tr>
			<td>
				@Html.DisplayFor(modelItem => item.CustomerID)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.CustomerTypeID)
			</td>
			<td>
				@*@Html.ActionLink("Edit", "Edit", "Customercustomerdemo", new { CustomerID = item.CustomerID }, new { htmlAttributes = new { @class = "form-control" } })*@
				@Html.ActionLink("Edit", "Edit", "Customercustomerdemo", new { CustomerID = item.CustomerID, CustomerTypeID = item.CustomerTypeID }, new { htmlAttributes = new { @class = "form-control" } })
			</td>
			<td>
				@*@Html.ActionLink("Details", "Details", "Customercustomerdemo", new { CustomerID = item.CustomerID }, new { htmlAttributes = new { @class = "form-control" } })*@
				@Html.ActionLink("Details", "Details", "Customercustomerdemo", new { CustomerID = item.CustomerID, CustomerTypeID = item.CustomerTypeID }, new { htmlAttributes = new { @class = "form-control" } })
			</td>
			<td>
				@*@Html.ActionLink("Delete", "Delete", "Customercustomerdemo", new { CustomerID = item.CustomerID }, new { @class = "btn btn-danger btn-sm" })*@
				@Html.ActionLink("Delete", "Delete", "Customercustomerdemo", new { CustomerID = item.CustomerID, CustomerTypeID = item.CustomerTypeID }, new { @class = "btn btn-danger btn-sm" })
			</td>
		</tr>
	}
</tbody>
<tfoot>
	<tr>
		<th colspan="5">
			<div class="row align-items-center">
				<div class="col-md-auto">
					Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
				</div>
				<div class="col-md-auto">
					@Html.PagedListPager(Model, page => Url.Action("Edit",
						new { ViewBag.PgeSize, ViewBag.CustomerTypeID, page, sortOrder = ViewBag.CurrentSort, currentFilter = ViewBag.CurrentFilter }))
				</div>
				<div class="col-md-auto">Page Size</div>
				<div class="col-md-auto">
					@Html.DropDownList("PgeSize", (IEnumerable<SelectListItem>)ViewBag.PgeSizeList, new { id = "ddpgesize", @class = "form-control btn-group-vertical" })
				</div>
			</div>
		</th>
	</tr>
</tfoot>
		

